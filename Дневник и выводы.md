
*23.05.26*

Чтобы кавычки были одинаковыми на всех устройствах надо задать четкий шрифт. На проекте `tailwind 4.1.13` так что шрифт устанавливается так
``` css
/* main.css */

/* Импорт шрифта должен быть выше всех остальных импортов */
@import url('https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap');  
@import "tailwindcss";

/* Переопределение встароеной переменной --font-sans */
@theme {  
  --font-sans: "Inter", sans-serif;  
}

```

---

*21.05.26*

делали со Стасом откат прода до состояния в origin репозитория
```
git fetch origin 
git reset --hard origin/main
```

---

Функции по умолчанию юзаешь с стора, все остальное через storeToRefs для удобства

Если деструктурировать store напрямую
``` js
const { user, posts } = useUserStore()
```
то переменные `user` и `posts` потеряют реактивность — они станут обычными значениями на момент деструктуризации и не будут обновляться при изменениях в store.

``` js
const userStore = useUserStore()

// Извлекаем реактивные ссылки на state и getters
const { name, email, fullName } = storeToRefs(userStore)

// Для actions деструктуризация без storeToRefs безопасна
const { updateUser } = userStore
```
 теперь можно использовать `name`, `email`, `fullName`, и они будут реактивно обновляться
 ---
 